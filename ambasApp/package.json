{
  "scripts": {
    "dev": "concurrently \"cd ../../transito_front && npm install && npm run dev\" \"cd .. && npm start\"",
    "build-all": "node build.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
